syntax = "proto3";

package Scanner.Net.Api.Common;

message LinuxTimeWithMs {
  // секундная часть времени (unix timestamp)
  uint64  sec     = 1;
  // миллисекундная часть времени (внутри секунды)
  uint32  ms      = 2;
}
